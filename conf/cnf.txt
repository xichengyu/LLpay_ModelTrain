
# raw_data = ../../data/raw_data.dt
raw_data = ../../data/ctrip_sample.dt

column_name = ../../conf/column_name.txt

log_path = model_result.log

# RF, GBDT, LR
algorithm = RF

# missing value strategy
strategies = ["mean", "median", "most_frequent"]

times_n = 20

columns = abc


